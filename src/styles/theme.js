const colors = {
  primary: "#FD4344",
  body: "#3D5A80",
};

const textColors = {
  dark: "#2c2d2d",
  white: "#FFF",
  sidebarText: "#FFF",
};

const padding = {
  small: "1rem",
  medium: "2rem",
};

const borderRadius = {
  card: "3px",
};

const boxShadow = {
  card: "2px 4px 10px rgba(0, 0, 0, 0.25)",
};

export { borderRadius, boxShadow, colors, padding, textColors };
